@model ResumeCreator.Models.ProfileData

@{
    ViewData["Title"] = "Nuevo";
}

<h1>Nuevo</h1>

<h4>DatosPersonales</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        @using (Html.BeginForm("New","DatosPersonales"))
        {
            <div class="form-group">
                <label for="NombreUsuario"> Nombre de Usuario </label>
                <input type="text" name="UserName" value="@Model.UserName" class="form-control" />
                @Html.ValidationMessageFor(x => x.UserName)
            </div>
            <div class="form-group">
                <label asp-for="Email" class="control-label"></label>
                <input asp-for="Email" class="form-control" />
                @Html.ValidationMessageFor(x => x.Email)
            </div>
            <div class="form-group">
                <label asp-for="DNI" class="control-label"></label>
                <input asp-for="DNI" class="form-control" />
                @Html.ValidationMessageFor(x => x.DNI)
            </div>
            <div class="form-group">
                <label asp-for="Age" class="control-label"></label>
                <input asp-for="Age" class="form-control" />
                @Html.ValidationMessageFor(x => x.Age)
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        }
        
    </div>
</div>

<div>
    @Html.ActionLink("Atras","Listado","DatosPersonales")
</div>


